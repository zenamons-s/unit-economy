# SaaS Unit Economics Dashboard

## Обнаружено в репозитории (факты)
- Основная точка входа — `app.py` (Streamlit-приложение) с вкладками Dashboard, Financial Planning, Actual Tracking, Variance Analysis, Scenario Simulation, AI Analyst, Reports и Settings.
- База данных SQLite создаётся/инициализируется автоматически при старте приложения через `db_manager.initialize_database()`; дефолтный путь — `database/saas_finance.db`.
- В репозитории есть отдельный CLI-скрипт инициализации БД `database/init_db.py` с командами `init`, `reset`, `backup`, `benchmarks`.
- Бизнес-логика разбита на модули `services/core`, `services/financial_system`, `services/utils`, а генераторы отчётов — в `reports/`.
- Есть интеграция с GigaChat через `gigachat_analyst.py` и использование переменных окружения/Streamlit secrets для конфигурации API.
- Экспорт данных реализован через `services/utils/export_generator.py` (Excel/CSV/JSON/PDF/HTML/Markdown).
- Тесты представлены файлами `test_company.py` и `test_database.py` (простые сценарии создания компании/БД).

---

## A) Описание проекта
SaaS Unit Economics Dashboard — это приложение на Streamlit для ведения unit-экономики SaaS-стартапа, планирования финансов на 12 месяцев, трекинга фактических данных и анализа отклонений. Пользователь работает через веб-интерфейс с несколькими вкладками (дашборды, планирование, сценарии, отчёты), а данные хранятся в SQLite-базе. Бизнес-логика и расчёты разнесены по сервисным слоям, а отчёты формируются отдельными генераторами в `reports/`.

## B) Возможности (по факту из кода)
- Dashboard с обзором метрик, alert-ами и рекомендациями (вкладка `🏠 Dashboard`).
- Финансовое планирование на 12 месяцев (вкладка `📈 Financial Planning`) через `FinancialPlanner`.
- Ввод и анализ фактических данных (вкладка `📊 Actual Tracking`) через `monthly_tracker`.
- Анализ отклонений план/факт (вкладка `🔍 Variance Analysis`) через `variance_analyzer`.
- Сценарное моделирование (вкладка `🎯 Scenario Simulation`) через `ScenarioSimulator`.
- AI-аналитик и рекомендации с опциональной интеграцией GigaChat (вкладка `🤖 AI Analyst`).
- Генерация отчётов для инвесторов/борда/менеджмента (вкладка `📋 Reports`).
- Экспорт планов, фактов и отчётов в Excel/CSV/JSON/PDF/HTML/Markdown через `export_generator`.

## C) Архитектура кратко
**Поток данных:** UI (Streamlit) → сервисы (core/financial_system/utils) → SQLite (db_manager) → отчёты/экспорт.

```
           ┌──────────────────────────┐
           │        Streamlit UI       │
           │  app.py (tabs/pages)      │
           └─────────────┬────────────┘
                         │
                         ▼
           ┌──────────────────────────┐
           │   services/* (logic)     │
           │ core / financial_system  │
           └─────────────┬────────────┘
                         │
                         ▼
           ┌──────────────────────────┐
           │ SQLite (db_manager.py)   │
           │ database/saas_finance.db │
           └─────────────┬────────────┘
                         │
                         ▼
           ┌──────────────────────────┐
           │ reports/ + exports        │
           │ investor/board/monthly    │
           └──────────────────────────┘
```

## D) Структура репозитория
```
.
├── app.py
├── database/
│   ├── db_manager.py
│   └── init_db.py
├── services/
│   ├── core/
│   ├── financial_system/
│   └── utils/
├── reports/
├── gigachat_analyst.py
├── requirements.txt
├── .streamlit/
│   ├── config.toml
│   └── secrets.toml
├── test_company.py
└── test_database.py
```

## E) Требования
- **Python:** версия не зафиксирована (нет `pyproject.toml` / `Pipfile` / `.python-version`).
  - Минимальная версия по коду точно не определена — ориентируйтесь на требования библиотек в `requirements.txt`.
- **Зависимости:** `requirements.txt` содержит Streamlit, pandas, numpy, plotly, reportlab, python-docx, pytest и др.

## F) Установка
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```
Если вы получаете сетевые ошибки pip — см. раздел Troubleshooting (там приведены общие рекомендации, но они **не закреплены** в проекте).

## G) Инициализация данных/БД
По умолчанию приложение использует SQLite по пути `database/saas_finance.db` (создаётся при запуске).

Опционально БД можно инициализировать вручную:
```bash
python database/init_db.py init
```
Сброс БД (полное удаление и пересоздание):
```bash
python database/init_db.py reset
```
Создание резервной копии:
```bash
python database/init_db.py backup
```

## H) Запуск приложения
```bash
streamlit run app.py
```

### Переменные окружения / secrets
Интеграция GigaChat читает параметры из переменных окружения и `st.secrets`:
- `GIGACHAT_CLIENT_ID`
- `GIGACHAT_CLIENT_SECRET`
- `GIGACHAT_SCOPE`
- `GIGACHAT_BASE_URL`
- `GIGACHAT_AUTH_URL`
- `GIGACHAT_VERIFY_SSL`

Эти параметры явно загружаются в `gigachat_analyst.py` (и могут быть заданы в `.streamlit/secrets.toml`).

В `.streamlit/secrets.toml` также присутствуют флаги `features.*`, `DB_PATH`, `LOG_LEVEL`, но в коде нет прямого чтения `DB_PATH` — при необходимости уточните назначение/использование этих параметров.

## I) Тестирование
В репозитории есть два тестовых сценария:
- `test_database.py` — функция `test_company_creation` (можно запускать через `pytest` или напрямую).
- `test_company.py` — скрипт с пошаговым созданием компании (запуск через `python test_company.py`).

Примеры запуска:
```bash
pytest
python test_company.py
```

## J) Troubleshooting
> **Важно:** в проекте нет закреплённых настроек pip mirrors/timeout. Ниже — **общие** рекомендации, которые следует подтвердить у команды.

- **Проблемы с сетью при `pip install`:**
  - Общее решение: `pip install -r requirements.txt --default-timeout=60`.
  - Если нужны зеркала — зафиксируйте их в `pip.conf` или README (сейчас это не описано).

- **Ошибки БД (нет файла/таблиц/locked):**
  - Убедитесь, что каталог `database/` существует.
  - Используйте `python database/init_db.py init` или `reset` для пересоздания БД.

- **Ошибки Streamlit (импорты/пути):**
  - Приложение добавляет корень проекта в `sys.path` в `app.py`, поэтому запускать нужно из корня репозитория.

- **Экспорт отчётов:**
  - Поддерживаемые форматы — Excel/CSV/JSON/PDF/HTML/Markdown (см. `export_generator`).
  - Если нужен DOCX, его реализация отсутствует в экспортере (см. раздел «Несоответствия»).

## K) Разработка/расширение
### Где добавлять новую метрику
- **Stage-aware метрики:** `services/core/stage_aware_metrics.py` хранит словари целевых метрик для стадий (Pre-seed/Seed/Series A...).
- **Финансовые вычисления:** план/факт и суммарные метрики формируются в `services/financial_system/financial_planner.py` и `services/financial_system/monthly_tracker.py`.

### Где добавлять новый отчёт/экспорт
- Шаблоны и логика отчётов: `reports/` (например, `investor_report.py`, `board_report.py`, `monthly_report.py`).
- Экспорт форматов: `services/utils/export_generator.py` (добавление нового формата/шаблона).

### Как добавить новую страницу/раздел в UI
- Навигация и вкладки определяются в `SAASDashboardApp.run()` в `app.py`. Добавьте новую вкладку в список `tabs` и соответствующий метод рендера (по аналогии с существующими `render_*`).

### Как добавить новую таблицу/миграцию в SQLite
- **Схема:** `database/db_manager.py` создаёт основные таблицы (`companies`, `financial_plans`, `monthly_plans`, `actual_data` и др.).
- **Расширенная схема и демо-данные:** `database/init_db.py` содержит дополнительные таблицы (scenarios, reports, capex_items и т.д.).

> **Рекомендация:** при добавлении новой таблицы обновляйте оба файла (если вы используете оба режима инициализации), либо унифицируйте подход, чтобы избежать расхождений.

## L) Не определено / нужно уточнить
- **Python version pinning:** отсутствует. Рекомендуется добавить `.python-version` или `pyproject.toml` (пример: `python_version = "3.10"`), чтобы зафиксировать окружение.
- **Деплой/релиз:** CI/CD не описан. Рекомендация — добавить `Dockerfile` и GitHub Actions workflow для линтинга/тестов.
- **pip mirrors/timeout:** не закреплены. Нужны договорённости и фиксация в README или `pip.conf`.
- **Использование `DB_PATH` из `.streamlit/secrets.toml`:** в коде не найдено прямого использования; если планируется — уточнить механизм передачи в `DatabaseManager`.

## M) Приложения
### .env.example (только если используете env vars)
```dotenv
GIGACHAT_CLIENT_ID=
GIGACHAT_CLIENT_SECRET=
GIGACHAT_SCOPE=GIGACHAT_API_PERS
GIGACHAT_BASE_URL=https://gigachat.devices.sberbank.ru/api/v1
GIGACHAT_AUTH_URL=https://ngw.devices.sberbank.ru:9443/api/v2/oauth
GIGACHAT_VERIFY_SSL=false
```
> Значения используются в `gigachat_analyst.py` и могут быть переопределены через `st.secrets`.

## Несоответствия (по сравнению с предыдущим README)
- **Экспорт в Word (DOCX):** в коде генератора экспортов нет реализации DOCX, хотя в `reports/investor_report.py` встречается `export_formats = ["pdf", "docx"]`. Требуется уточнение/реализация экспорта DOCX в `export_generator`.

## Список уточняющих вопросов владельцу проекта
1) Какую версию Python нужно поддерживать и нужно ли зафиксировать её в `.python-version`/`pyproject.toml`?
2) Должен ли `DB_PATH` из `.streamlit/secrets.toml` использоваться приложением? Если да — как его прокидывать в `DatabaseManager`?
3) Нужна ли реальная поддержка DOCX-экспорта отчётов (сейчас формат указан, но реализация отсутствует)?
4) Какой процесс релиза/деплоя планируется (Docker, CI/CD, облако)?
5) Должны ли быть задокументированы настройки pip mirrors/timeout для вашей инфраструктуры?
6) Нужна ли миграционная стратегия для SQLite (скрипты, Alembic или простые SQL-миграции)?
